---
// M_FilterChips — молекула. = Figma M_FilterChips (node 23:1912). Сетка чипов-
// фильтров (A_Tag). Чипы передаются слотом. Используется в сайдбаре Кейсов (верх).
import styles from './M_FilterChips.module.css';

interface Props {
  class?: string;
}
const { class: extra } = Astro.props;
---

<div class:list={[styles.chips, extra]}>
  <slot />
</div>
