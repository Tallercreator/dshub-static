---
// W_ResourcesGrid — обёртка. Бенто-сетка ресурсов (Figma W_ResourcesGrid 30:4839):
// 3 колонки, ряды по 280px, gap 24. Первая карточка (size=large) занимает 2 колонки.
// Desktop 3 кол / ≤1024 2 / ≤640 1. Контент слотом.
import styles from './W_ResourcesGrid.module.css';

interface Props {
  class?: string;
}
const { class: extra } = Astro.props;
---

<div class:list={[styles.grid, extra]}><slot /></div>
